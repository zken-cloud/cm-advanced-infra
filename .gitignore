terraform.tfvars
# written by tf-init.sh; names your project's state bucket
backend.hcl
.terraform/
*.tfstate
*.tfstate.*
tfplan

# build artefacts from the payload
lab/**/__pycache__/
lab/**/.pytest_cache/
lab/**/.terraform/
lab/**/*.tfstate*
# the CodeMender CLI is copied in at build time by bootstrap.sh; never committed
lab/infra/runner-image/cm
